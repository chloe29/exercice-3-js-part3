//  on déclare la fonction dont l'évènement appelle onclick que l'on a nommé couleur
// on modifie le document html avec la méthode getElementById en récupérant l'id, on lui applique le style color
function couleur(){
  document.getElementById('text').style.color='green';
}
//  on déclare la fonction dont l'évènement appelle onclick que l'on a nommé changer
// on modifie le document html avec la méthode getElementById en récupérant l'id, on lui applique le style color
function changer(){
  document.getElementById('text').style.color='red';
}
//  on déclare la fonction dont l'évènement appelle onclick que l'on a nommé changement
// on modifie le document html avec la méthode getElementById en récupérant l'id, on lui applique le style color
function changement(){
  document.getElementById('text').style.color='blue';
}
